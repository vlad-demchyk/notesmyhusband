{
    "name": "network-notebook/api",
    "description": "Network Notebook API with GraphQL and SQLite",
    "type": "project",
    "require": {
        "php": ">=7.4",
        "webonyx/graphql-php": "^15.0",
        "firebase/php-jwt": "^6.0"
    },
    "autoload": {
        "psr-4": {
            "App\\": "api/"
        }
    },
    "scripts": {
        "serve": "php -S localhost:8000 router.php",
        "start": "@serve"
    }
}
